# Slacker News Web

This workspace contains the Astro frontend for Slacker News.

Article content no longer lives in `src/content/posts`. The site now reads published posts from the Payload CMS API and keeps Astro content only for static pages like `About` and `Submissions`.

## Commands

```bash
bun run dev
bun run build
bun run check
```
